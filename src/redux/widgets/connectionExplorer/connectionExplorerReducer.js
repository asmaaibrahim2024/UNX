import { ActionTypes } from "../../constants/actionTypes";

const initialState = {

 };

export const connectionExplorerReducer = (state = initialState, { type, payload }) => {
     switch (type) {
  
         default:
            return state; 
    }
} 