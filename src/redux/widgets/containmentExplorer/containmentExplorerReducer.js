import { ActionTypes } from "../../constants/actionTypes";

const initialState = {

 };

export const containmentExplorerReducer = (state = initialState, { type, payload }) => {
     switch (type) {
  
         default:
            return state; 
    }
} 