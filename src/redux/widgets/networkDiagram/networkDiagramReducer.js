import { ActionTypes } from "../../constants/actionTypes";

const initialState = {

 };

export const networkDiagramReducer = (state = initialState, { type, payload }) => {
     switch (type) {
  
         default:
            return state; 
    }
} 