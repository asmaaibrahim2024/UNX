import { ActionTypes } from "../../constants/actionTypes";

const initialState = {

 };

export const layerListReducer = (state = initialState, { type, payload }) => {
     switch (type) {

         default:
            return state; 
    }
} 