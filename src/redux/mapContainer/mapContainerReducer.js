import { ActionTypes } from "../constants/actionTypes";

const initialState = {
};

export const mapContainerReducer = (state = initialState, { type, payload }) => {
     switch (type) {

         default:
            return state; 
    }
} 