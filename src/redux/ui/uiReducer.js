import { ActionTypes } from "../constants/actionTypes";

const initialState = {
};

export const uiReducer = (state = initialState, { type, payload }) => {
    switch (type) {
       
        default:
            return state; 
    }
} 
